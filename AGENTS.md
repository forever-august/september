# Overview

September is a HTTP to NNTP gateway service written in rust using the Axum library.
It also has an integration test suite written in python using selenium and docker.

## Planning
Always load the feature-file skill before interacting with features.yml

Always use the features.yml file to track planning and implementation work.
Every requirement **MUST** have one or more test cases. Both happy path and failure modes should be tested.